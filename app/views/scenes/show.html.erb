<div class="container">
  <div class="show-card">
    <h3><%= @scene.name %></h3>
    <%= link_to scene_path(@scene), method: :delete, data: { confirm: "Are you sure?" } do %>
      <i class="fa fa-trash destroy_dose"></i>
    <% end %>
  </div>

  <hr>
  <%= link_to "back", script_path(@script)  %>
</div>

